import styled from "styled-components";

export const LastReviewedContainer = styled.div`
  border-top: 0.75px solid var(--clr-stroke);
  padding-top: 40px;
`;

export const Title = styled.h2`
  font-size: 20px;
  font-weight: 600;

  margin-bottom: -16px;
`;
